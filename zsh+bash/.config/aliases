# alternatives
alias v='vim'
TERMEMU=$(ps -p $(ps -p $$ -o ppid=) o args=)
if [ "$TERMEMU" != "cool-retro-term" ]; then
unalias v
alias v='nvim'
alias vim='nvim'
fi
alias t='tree -L 2'
alias ne='/usr/bin/emacs -nw'
alias e='edit'
alias c='cat'
alias vime='ne'
alias emacs='ne'
alias cls='clear'
alias motd='fortune'
alias nyan='nyancat'
alias py='python3'
alias py2='python2'
alias blic='blihc'
alias ch='chmod'
alias svp='sudo'
alias stp='sudo'
alias please='sudo'
alias fucking='sudo'
alias bliclone='blihclone'
alias blido='blihdo'
alias cz='chgsize'
alias écho='echo'
alias ls='ls --color=always'
alias grep='grep --color=auto'
alias o='/bin/vim -u /usr/share/vim/vim80/macros/less.vim'

# options shorteners
alias l='ls -1 '
alias ll='ls -lh '
alias wifi='gnome-control-center network '
alias sound='gnome-control-center sound'
alias glog="git log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %C(bold blue)<%an>%C(yellow)%d%Creset' --abbrev-commit "
alias recmic='arecord -vv -fdat '
alias gs='git status --short '
alias gd='git diff '
alias gay='lolcat -a -d 3 '

# commands shorteners
alias sudo='sudo '
alias blog='cd ~/repos/skielred/blog '
alias uu='dnf upgrade --refresh '
alias nyancat='telnet nyancat.dakko.us '
alias pip3uu='pip3 freeze --local | grep -v '^\-e' | cut -d = -f 1  | xargs -n1 pip3 install -U'

# replacements
# alias ls='exa '
alias tree='exa -T '

# functions
chgsize() {
    if [ $(($1%2)) -eq 1 ]; then
        echo You must input an even number
        return
    fi
	printf '\33]50;%s\007' "xft:Monaco:size=$1"
}

edit() {
    $EDITOR $1
}

ghclone() {
	if [ -z $2 ]; then
		git clone git@github.com:"$1"
	else
		git clone git@github.com:"$1" "$2"
	fi
}

mkcd() {
    mkdir -p $1
    cd $1
}

# vim:ft=sh
