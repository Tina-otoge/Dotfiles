#!/usr/bin/env bash

path='/tmp/bg.png'
logo="$HOME"'/.config/i3/logolock.png'

scrot -e 'mv $f '"$path"

if [ -f $logo ]; then
    composite "$logo" -gravity NorthEast "$path" "$path"
fi

i3lock -n -f -i "$path"
